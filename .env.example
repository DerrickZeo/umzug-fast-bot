# Credentials
LOGIN_USERNAME=your_username_here
LOGIN_PASSWORD=your_password_here

# Base URL (canonical)
BASE_URL=https://studenten-umzugshilfe.com

# Headless mode (true/false)
HEADLESS=true

# Watch interval (ms)
POLL_MS=1000

# Accept at most N new entries per tick
MAX_PER_TICK=3

# Keep-alive interval (minutes)
KEEP_ALIVE_MIN=4

# HTTP port for health endpoint
PORT=3000
